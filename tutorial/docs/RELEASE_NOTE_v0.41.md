# Grandis Legacy Tutorial v0.41

Date: 2026-08-24

This patch renames the two bundled audio assets to `Coin Flip.mp3` and `Card Sound.mp3`, updates their executable references and cache/source-lock metadata, and preserves the exact audio bytes and playback behavior.

Tutorial gameplay remains synchronized with the unchanged VS AI v6.9 gameplay authority. The teaching flow, phase guidance, Arvon, mobile/desktop behavior, VFX, and EXP guidance are unchanged.

Run `npm test` from the Tutorial directory or `npm run verify` from the parent repository.
