'use strict';
function tagSet(card){const raw=card&& (card.runtime_tags||card.canonical_legality&&card.canonical_legality.runtime_tags)||[];if(Array.isArray(raw))return new Set(raw.map(x=>String(x).toUpperCase()));return new Set(String(raw).split(/[;,]/).map(x=>x.trim().toUpperCase()).filter(Boolean));}
function sourceExhaustsAfterSuccessfulUse(card,context){const x=card&& (card.canonical_execution||card.rules&&card.rules.execution)||{};if(x.source_exhausts_after_successful_use!==undefined)return x.source_exhausts_after_successful_use===true;const family=String(card&& (card.card_family||card.card_type||card.family)||'').toLowerCase();const classification=String(card&& (card.card_subtype||card.classification)||'').toLowerCase();const tags=tagSet(card);if(family.includes('item')||family.includes('event'))return false;if(context&&context.racial_trait)return false;if(classification.includes('defense')||context&&context.response)return false;if(tags.has('NO_EXHAUST')||tags.has('NO_EXHAUST_FOR_ULTIMATE'))return false;return family.includes('skill');}
function usableWhileExhausted(card,context){const l=card&& (card.canonical_legality||card.rules&&card.rules.legality)||{};const sr=l.source_requirement||{};if(sr.usable_while_exhausted===true)return true;if(context&& (context.response||context.racial_trait))return true;return false;}
module.exports={sourceExhaustsAfterSuccessfulUse,usableWhileExhausted};
