# Grandis Legacy VS AI v5.54

## Changes
- `Ultimate Rules` now shows only the Bound Hero sentence and 200 EXP Tribute value.
- Preserves the Noto Sans `@font-face` asset paths so an existing/manual font folder remains compatible; font binaries are not bundled.
- Returns `AI LOBBY` to the previous compact size while vertically centering it with the logo.
- Preserves Stoneblood continuation, Hero Progression, confirmed-defeat cleanup, and the v5.53 runtime stack.

## Active stack
Runtime Foundation v1.81 · Runtime Core v0.49 · Shared Runtime Manual v1.37 · Application Runtime Sync v2.41 · UI Design Lock v2.45.

Open `index.html` through GitHub Pages.
