'use strict';
const fs=require('fs'),path=require('path'),vm=require('vm');const ROOT=path.resolve(__dirname,'..');const must=(v,m)=>{if(!v)throw new Error(m)};const read=r=>fs.readFileSync(path.join(ROOT,r),'utf8');
const ctx={};ctx.globalThis=ctx;ctx.window=ctx;vm.createContext(ctx);vm.runInContext(read('shared-ui/battlefield-ui.js'),ctx,{filename:'shared-ui/battlefield-ui.js'});const ui=ctx.GL_SHARED_BATTLEFIELD_UI;must(ui&&ui.version==='v2.52','Shared Battlefield UI v2.52 missing');must(ui.visual_baseline==='VS AI v6.42','visual baseline must be VS AI v6.42');must(ui.previewPointerEvents==='none','preview pointer contract must be none');must(ui.counterAssetScope==='mana-regen-only','counter scope mismatch');must(ui.deckCountPresentation==='numeric-above-card','deck count presentation mismatch');
for(const [w,h] of [[1280,720],[1024,768],[1600,900]]){const p=ui.desktopPreviewPosition(w,h,250,350);must(p.x>=8&&p.y>=8&&p.x+250<=w-8+1&&p.y+350<=h-8+1,`preview out of viewport ${w}x${h}`);must(p.x>w/2,`preview not in right-side region ${w}x${h}`);}
const css=read('shared-ui/battlefield-ui.css');must(/is-v642-right-preview\{[^}]*pointer-events:none!important/i.test(css),'right preview CSS must be non-interactive');must(/width:250px!important/.test(css)&&/height:350px!important/.test(css),'desktop preview must remain 250x350');
for(const rel of ['index.html','tutorial/index.html']){const h=read(rel);must(/shared-ui\/battlefield-ui\.js/.test(h),rel+' missing shared UI JS');must(/shared-ui\/battlefield-ui\.css/.test(h),rel+' missing shared UI CSS');must(/shared-app\/app\.bundle\.js/.test(h),rel+' does not consume the shared application source');must(/shared-app\/app\.css/.test(h),rel+' does not consume shared common battlefield CSS');must(/shared-app\/battlefield-authority\.css/.test(h),rel+' missing neutral battlefield authority stylesheet');must(!/lab-authority\.css/.test(h),rel+' still loads retired Playtest Lab stylesheet name');}
const sharedApp=read('shared-app/app.bundle.js'),deployment=read('shared-app/active-starters.js')+sharedApp;must(read('js/app.bundle.js')===deployment&&read('tutorial/js/app.bundle.js')===deployment,'generated app deployment mirrors drift from active-starter prelude + one editable shared source');must(read('css/app.css')===read('shared-app/app.css')&&read('tutorial/css/app.css')===read('shared-app/app.css'),'generated common CSS mirrors drift from one editable shared source');
for(const rel of ['shared-app/app.bundle.js','js/app.bundle.js','tutorial/js/app.bundle.js']){const s=read(rel);must(s.includes('v642DesktopPreviewShow'),'desktop right-side preview implementation missing in '+rel);must(s.includes("box.style.pointerEvents='none'"),'preview pointer-events runtime guard missing in '+rel);must(s.includes("pointerleave',function(ev){if(!ev||ev.pointerType!=='touch')v94HoverZoomHide();"),'source-card immediate mouseleave missing in '+rel);must(!s.includes('id="deckSetupButton"')&&!s.includes('id="mobileDeckSetupButton"'),'battlefield Deck Setup button markup still active in '+rel);must(s.includes('showDeckSetup()')&&s.includes("$('startFromControl')"),'pre-match Deck Setup system missing in '+rel);must(s.includes('gl-deck-count--above'),'numeric above-deck count missing in '+rel);must(s.includes('gl-status-count'),'numeric status count missing in '+rel);must(!/gl-status-die/.test(s),'status still uses graphical Counter asset in '+rel);}
console.log('PASS Shared Battlefield UI v2.52: right-side overlay geometry, non-interactive immediate hover lifetime, Deck Setup battlefield removal, numeric deck/status counts.');
