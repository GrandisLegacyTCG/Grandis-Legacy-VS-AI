'use strict';
const {read,assert}=require('../_helpers.cjs');const tribute=require('../../../runtime-source/runtime/core/tribute-policy.js');
const cards=read('data/season1/cards.runtime.v0.16.0.json').cards;const ult=cards.find(c=>c.canonical_execution&&c.canonical_execution.is_ultimate);assert(ult,'Ultimate fixture missing');const ownerIds=(ult.canonical_legality?.source_requirement?.ultimate?.owner_lineage_card_ids||ult.canonical_execution?.tribute?.owner_lineage_card_ids||[]);assert(ownerIds.length,'Ultimate owner lineage fixture missing');const cls=require('../../../runtime-source/runtime/core/mana-deck-policy.js').classFromCardCode(ult.card_id);const matching={mana_id:'matching',owner_id:'P1',kind:'CLASS',class_name:cls};const player={player_id:'P1',mana_pool_cards:[matching,{mana_id:'generic',owner_id:'P1',kind:'GENERIC'}],mana_deck:[{mana_id:'older-batch',owner_id:'P1',kind:'GENERIC'}],mana_pool:2,mana_regen:1};const result=tribute.executeUltimateTributeCost(ult,ownerIds[0],player);assert(result.ok,'Ultimate tribute cost must execute');assert(player.mana_pool_cards.every(x=>x.mana_id!=='matching'),'matching Class Shard must leave pool');assert(player.mana_deck[player.mana_deck.length-1].mana_id==='matching','used matching Class Shard must return deepest/last in its payment batch at bottom of Shard Deck');assert(player.mana_deck[0].mana_id==='older-batch','later payment must not reorder older return/deck segment');console.log('PASS Ultimate Shard return integration through Shared Runtime');
